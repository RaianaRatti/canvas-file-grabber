name: Build and Release

on:
  release:
    types: [created]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
            output_name: CanvasFileGrabber.exe
            asset_name: CanvasFileGrabber-Windows.exe
          - os: macos-latest
            output_name: CanvasFileGrabber
            asset_name: CanvasFileGrabber-Mac.zip
          - os: ubuntu-latest
            output_name: CanvasFileGrabber
            asset_name: CanvasFileGrabber-Linux.zip

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
          playwright install chromium

      - name: Build with PyInstaller (Windows)
        if: runner.os == 'Windows'
        run: |
          pyinstaller --name CanvasFileGrabber --windowed --onefile `
            --collect-all playwright --collect-all webview `
            --add-data "web;web" --add-data "config.example.json;." `
            run.py

      - name: Build with PyInstaller (macOS)
        if: runner.os == 'macOS'
        run: |
          pyinstaller --name CanvasFileGrabber --windowed --onefile \
            --collect-all playwright --collect-all webview \
            --add-data "web:web" --add-data "config.example.json:." \
            run.py

      - name: Build with PyInstaller (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-gi gir1.2-webkit2-4.1
          pyinstaller --name CanvasFileGrabber --windowed --onefile \
            --collect-all playwright --collect-all webview \
            --add-data "web:web" --add-data "config.example.json:." \
            run.py

      - name: Zip macOS build
        if: runner.os == 'macOS'
        run: |
          cd dist
          zip -r ../CanvasFileGrabber-Mac.zip CanvasFileGrabber.app
          cd ..

      - name: Zip Linux build
        if: runner.os == 'Linux'
        run: |
          cd dist
          zip -r ../CanvasFileGrabber-Linux.zip CanvasFileGrabber
          cd ..

      - name: Upload assets to release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/CanvasFileGrabber.exe
            CanvasFileGrabber-Mac.zip
            CanvasFileGrabber-Linux.zip